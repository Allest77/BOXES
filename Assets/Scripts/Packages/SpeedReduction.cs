using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpeedReduction : MonoBehaviour
{
    /*
        void Update()
        {
            //A route variable that holds a random # of packages per stop.
            int packageIndex = Random.Range(1, 33); //It will never go up to 33.

            for (int i = 0; i < packageIndex; i++)
            {

            }
        }

        void SpeedReduction() {
            //Speed Reduction Variables (slows down the player's speed. Subtract player's current speed by amount variables);
            public float minAmount = , envelopeAmount = , plasticAmount = , sBoxAmount = , mBoxAmount = , lBoxAmount = , maxAmount = ;
            
            //The customAmount is determined by a random range of weights.
            //float customAmount = Random.Range(packagePrefabs[minAmount], packagePrefabs[envelopeAmount], packagePrefabs[plasticAmount], packagePrefabs[sBoxAmount], packagePrefabs[mBoxAmount], packagePrefabs[lBoxAmount], packagePrefabs[maxAmount]);

    /* 
    //Reference Third Person Controller Script:
    private ThirdPersonMovement player;
    player = pc.GetComponent<ThirdPersonMovement>(); //we need to reference the player's speed, taken from this script.
    player.speed = 16;

    } */
}